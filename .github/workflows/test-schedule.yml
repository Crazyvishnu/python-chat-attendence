name: Test Scheduler (every 5 minutes)

on:
  schedule:
    - cron: '*/5 * * * *'   # every 5 minutes (UTC)
  workflow_dispatch: {}

jobs:
  echo-time:
    runs-on: ubuntu-latest
    steps:
      - name: Print time
        run: date -u && echo "Scheduled run OK"
